{
  "title": "Resize a folder of images with Pillow while keeping the originals",
  "summary": "Resize multiple images into a new folder without stretching them or replacing the source files. A small synthetic image set makes the expected output dimensions easy to calculate by hand.",
  "tags": [
    "Python",
    "Pillow",
    "Images"
  ],
  "audience": "This guide is for people who need smaller copies of many images while preserving each image's proportions and leaving the originals untouched.",
  "prerequisites": [
    "Python 3.12 and a terminal command that starts that version.",
    "Pillow installed with python -m pip install Pillow.",
    "A working folder where the scripts can create new folders beneath outputs.",
    "Enough disk space for both the original synthetic images and the resized copies."
  ],
  "sections": [
    {
      "id": "define-the-resize-rule",
      "heading": "1. Define the resizing rule first",
      "blocks": [
        {
          "type": "p",
          "text": "The goal is to fit every image inside a 300 by 300 pixel box while preserving its aspect ratio. Images that are already smaller than that box are not enlarged. The source images remain unchanged, and every resized copy is written to a separate output folder."
        },
        {
          "type": "p",
          "text": "For an image with width W and height H, calculate scale as the smaller of 300/W, 300/H, and 1. Then calculate the new width and height from that single scale factor. Using one factor for both dimensions prevents stretching."
        },
        {
          "type": "code",
          "text": "scale = min(300 / width, 300 / height, 1)\nnew_width = int(width * scale)\nnew_height = int(height * scale)"
        },
        {
          "type": "callout",
          "title": "This guide does not crop",
          "text": "A wide or tall image is fitted inside the box. Empty space around the fitted image is not added, and no pixels are cropped merely to force an exact 300 by 300 result."
        }
      ]
    },
    {
      "id": "create-synthetic-images",
      "heading": "2. Create a synthetic image folder",
      "blocks": [
        {
          "type": "p",
          "text": "The following files are synthetic and were created specifically for this article. The setup script makes four simple PNG images with known dimensions. Their visual content is unimportant; the dimensions are chosen so that every expected resize result can be checked by hand."
        },
        {
          "type": "code",
          "text": "from pathlib import Path\nfrom PIL import Image\n\nSOURCE = Path(\"outputs\") / \"image_resize_demo\"\nIMAGES = {\n    \"landscape.png\": ((800, 600), (220, 80, 80)),\n    \"portrait.png\": ((600, 800), (80, 120, 220)),\n    \"small.png\": ((200, 100), (80, 180, 100)),\n    \"wide.png\": ((1200, 300), (180, 120, 60)),\n}\n\nSOURCE.parent.mkdir(parents=True, exist_ok=True)\nSOURCE.mkdir()  # Stop if this synthetic source already exists.\n\nfor filename, (size, color) in IMAGES.items():\n    image = Image.new(\"RGB\", size, color)\n    target = SOURCE / filename\n    image.save(target, format=\"PNG\")\n"
        },
        {
          "type": "code",
          "text": "python create_resize_demo.py"
        },
        {
          "type": "table",
          "headers": [
            "File",
            "Original width",
            "Original height",
            "Aspect ratio"
          ],
          "rows": [
            [
              "landscape.png",
              "800",
              "600",
              "4:3"
            ],
            [
              "portrait.png",
              "600",
              "800",
              "3:4"
            ],
            [
              "small.png",
              "200",
              "100",
              "2:1"
            ],
            [
              "wide.png",
              "1200",
              "300",
              "4:1"
            ]
          ]
        },
        {
          "type": "p",
          "text": "The four originals contain 1,340,000 pixels in total: 480,000 + 480,000 + 20,000 + 360,000. Pixel count is useful for understanding the example, but it is not the same as file size in bytes because PNG compression depends on image content."
        }
      ]
    },
    {
      "id": "calculate-expected-sizes",
      "heading": "3. Calculate the four expected dimensions by hand",
      "blocks": [
        {
          "type": "p",
          "text": "For landscape.png, the limiting dimension is width: 300/800 = 0.375. Multiplying both dimensions by 0.375 gives 300 by 225. For portrait.png, height is limiting: 300/800 = 0.375, producing 225 by 300."
        },
        {
          "type": "p",
          "text": "small.png already fits because both dimensions are at or below 300. The scale is capped at 1, so it remains 200 by 100. For wide.png, width limits the result: 300/1200 = 0.25, giving 300 by 75."
        },
        {
          "type": "table",
          "headers": [
            "File",
            "Scale",
            "Expected output",
            "Output pixels"
          ],
          "rows": [
            [
              "landscape.png",
              "0.375",
              "300 × 225",
              "67,500"
            ],
            [
              "portrait.png",
              "0.375",
              "225 × 300",
              "67,500"
            ],
            [
              "small.png",
              "1",
              "200 × 100",
              "20,000"
            ],
            [
              "wide.png",
              "0.25",
              "300 × 75",
              "22,500"
            ]
          ]
        },
        {
          "type": "p",
          "text": "The resized copies therefore contain 177,500 pixels in total. The originals are still present separately, so this number is not a statement about disk-space savings."
        }
      ]
    },
    {
      "id": "resize-the-folder",
      "heading": "4. Resize the folder into a new destination",
      "blocks": [
        {
          "type": "p",
          "text": "Save the following script as batch_image_resize.py. It processes PNG, JPEG, and WebP files directly inside SOURCE. It does not recursively search subfolders. The destination is created with mkdir without exist_ok, so an existing output directory causes the script to stop instead of mixing new results with an earlier run."
        },
        {
          "type": "code",
          "text": "from pathlib import Path\nfrom PIL import Image, ImageOps\n\nSOURCE = Path(\"outputs\") / \"image_resize_demo\"\nOUTPUT_DIR = Path(\"outputs\") / \"image_resize_result\"\nMAX_WIDTH = 300\nMAX_HEIGHT = 300\nSUPPORTED = {\".png\", \".jpg\", \".jpeg\", \".webp\"}\n\n\ndef target_size(width: int, height: int) -> tuple[int, int]:\n    scale = min(MAX_WIDTH / width, MAX_HEIGHT / height, 1)\n    new_width = max(1, int(width * scale))\n    new_height = max(1, int(height * scale))\n    return new_width, new_height\n\n\ndef main() -> None:\n    if not SOURCE.is_dir():\n        raise FileNotFoundError(f\"Source folder not found: {SOURCE}\")\n    if MAX_WIDTH < 1 or MAX_HEIGHT < 1:\n        raise ValueError(\"Maximum dimensions must be positive integers.\")\n\n    source_resolved = SOURCE.resolve()\n    output_resolved = OUTPUT_DIR.resolve()\n    if source_resolved == output_resolved:\n        raise ValueError(\"SOURCE and OUTPUT_DIR must be different.\")\n\n    OUTPUT_DIR.parent.mkdir(parents=True, exist_ok=True)\n    OUTPUT_DIR.mkdir()  # Refuse to reuse an existing destination.\n\n    candidates = sorted(\n        path for path in SOURCE.iterdir()\n        if path.is_file() and path.suffix.lower() in SUPPORTED\n    )\n\n    resized_count = 0\n    unchanged_size_count = 0\n\n    for source_path in candidates:\n        output_path = OUTPUT_DIR / source_path.name\n        if output_path.exists():\n            raise FileExistsError(f\"Output already exists: {output_path}\")\n\n        with Image.open(source_path) as opened:\n            image = ImageOps.exif_transpose(opened)\n            image.load()\n            old_size = image.size\n            new_size = target_size(*old_size)\n\n            if new_size == old_size:\n                result = image.copy()\n                unchanged_size_count += 1\n            else:\n                result = image.resize(new_size, Image.Resampling.LANCZOS)\n                resized_count += 1\n\n            save_options = {}\n            if source_path.suffix.lower() in {\".jpg\", \".jpeg\"}:\n                save_options[\"quality\"] = 90\n            result.save(output_path, **save_options)\n            result.close()\n\n        print(f\"{source_path.name}: {old_size[0]}x{old_size[1]} -> {new_size[0]}x{new_size[1]}\")\n\n    print(f\"Processed {len(candidates)} images.\")\n    print(f\"Resized: {resized_count}; already within limit: {unchanged_size_count}.\")\n    print(f\"Output folder: {OUTPUT_DIR.as_posix()}\")\n\n\nif __name__ == \"__main__\":\n    main()\n"
        },
        {
          "type": "p",
          "text": "ImageOps.exif_transpose applies an image's EXIF orientation before the resize calculation when such orientation data exists. The synthetic PNG files have no such orientation requirement, so their displayed dimensions remain the dimensions listed above."
        }
      ]
    },
    {
      "id": "expected-result",
      "heading": "5. Compare the expected output",
      "blocks": [
        {
          "type": "p",
          "text": "Because filenames are processed in sorted order, the expected console lines are landscape.png, portrait.png, small.png, and wide.png. Three images become smaller and one remains at its original dimensions."
        },
        {
          "type": "code",
          "text": "landscape.png: 800x600 -> 300x225\nportrait.png: 600x800 -> 225x300\nsmall.png: 200x100 -> 200x100\nwide.png: 1200x300 -> 300x75\nProcessed 4 images.\nResized: 3; already within limit: 1.\nOutput folder: outputs/image_resize_result"
        },
        {
          "type": "p",
          "text": "The execution matched the expected output. Check the dimensions, the file count, and preservation of the original images in outputs/image_resize_demo."
        },
        {
          "type": "table",
          "headers": [
            "Check",
            "Expected value"
          ],
          "rows": [
            [
              "Files in source folder",
              "4"
            ],
            [
              "Files in output folder",
              "4"
            ],
            [
              "Resized files",
              "3"
            ],
            [
              "Files already within the limit",
              "1"
            ],
            [
              "Largest output width",
              "300"
            ],
            [
              "Largest output height",
              "300"
            ]
          ]
        }
      ]
    },
    {
      "id": "verify-files-yourself",
      "heading": "6. Verify the generated images yourself",
      "blocks": [
        {
          "type": "p",
          "text": "Do not rely only on filenames or console text. Open the resulting files and inspect their dimensions. The following optional checker reads both folders and reports dimensions. It performs no writes."
        },
        {
          "type": "code",
          "text": "from pathlib import Path\nfrom PIL import Image\n\nSOURCE = Path(\"outputs\") / \"image_resize_demo\"\nOUTPUT_DIR = Path(\"outputs\") / \"image_resize_result\"\n\nfor source_path in sorted(SOURCE.glob(\"*.png\")):\n    output_path = OUTPUT_DIR / source_path.name\n    with Image.open(source_path) as original, Image.open(output_path) as resized:\n        print(\n            f\"{source_path.name}: \"\n            f\"original={original.size}, output={resized.size}\"\n        )\n"
        },
        {
          "type": "ul",
          "items": [
            "Confirm that every output width is at most 300 and every output height is at most 300.",
            "Confirm that landscape.png remains 4:3, portrait.png remains 3:4, and wide.png remains 4:1.",
            "Confirm that small.png remains 200 by 100 instead of being enlarged to 300 by 150.",
            "Compare modification times or hashes if you need additional evidence that the source files themselves were not rewritten.",
            "Run the resize script a second time without changing OUTPUT_DIR. It should stop with FileExistsError before processing another batch."
          ]
        }
      ]
    },
    {
      "id": "common-errors",
      "heading": "7. Recognize common errors",
      "blocks": [
        {
          "type": "table",
          "headers": [
            "Symptom",
            "What to check"
          ],
          "rows": [
            [
              "ModuleNotFoundError: No module named PIL",
              "Install Pillow into the same Python environment used to run the script with python -m pip install Pillow."
            ],
            [
              "FileNotFoundError",
              "Check SOURCE and the terminal's current working directory. Run the synthetic setup script first for this example."
            ],
            [
              "FileExistsError",
              "The output folder already exists. Review it and select a fresh destination rather than allowing an automatic overwrite."
            ],
            [
              "Image cannot be identified",
              "The extension may look supported while the contents are damaged or are not actually an image. Treat the failed batch as incomplete."
            ],
            [
              "Unexpected rotation",
              "Orientation metadata and pixel orientation can differ. ImageOps.exif_transpose is used before resizing, but inspect important images visually."
            ],
            [
              "Unexpected output file size in bytes",
              "Pixel dimensions alone do not determine compressed file size. Format, image content, metadata, and JPEG quality also matter."
            ]
          ]
        },
        {
          "type": "p",
          "text": "If processing fails after some files have been written, the script does not delete those partial results. Keep the destination for investigation or use a new output directory after correcting the problem."
        }
      ]
    },
    {
      "id": "limits-before-real-use",
      "heading": "8. Understand the limits before using real images",
      "blocks": [
        {
          "type": "p",
          "text": "This example handles only files directly inside one folder and supports PNG, JPEG, and WebP extensions. It does not reproduce a nested directory tree, resize animated frames individually, preserve every form of metadata, or guarantee that color-management information survives unchanged."
        },
        {
          "type": "p",
          "text": "Resampling changes pixel data. Even when an image keeps the same dimensions, opening and saving it can change its encoded bytes or metadata. JPEG output is lossy and this script saves JPEG copies with quality 90; that does not preserve the original JPEG bitstream or original compression settings."
        },
        {
          "type": "p",
          "text": "The int calculation rounds fractional dimensions downward. For arbitrary source sizes, the resulting pixel ratio can therefore differ slightly from the mathematical ratio because image dimensions must be whole numbers. The synthetic dimensions in this guide divide exactly, so the four expected results require no ambiguous rounding."
        }
      ]
    }
  ],
  "verification": {
    "env": "ChatGPT Python tool · Python 3.13.5 · Pillow 12.3.0",
    "items": [
      "Created landscape.png 800×600, portrait.png 600×800, small.png 200×100, and wide.png 1200×300.",
      "The actual source pixel total is 1,340,000, not 1,240,000.",
      "The resize run produced exactly 300×225, 225×300, 200×100, and 300×75.",
      "The output pixel total is 177,500, matching the article.",
      "Console output exactly matched the expected seven lines, including 'Processed 4 images.' and 'Resized: 3; already within limit: 1.'.",
      "The optional checker printed the expected original and output dimensions for all four files.",
      "There were 4 files in the source folder and 4 files in the output folder.",
      "SHA-256 hashes of all four source files were unchanged after resizing, confirming the originals were not rewritten.",
      "Running the resize script again with the same OUTPUT_DIR raised FileExistsError before processing another batch.",
      "Local recheck on 2026-09-21 with Python 3.12.14: the corrected example passed; source preservation and overwrite protection also passed."
    ],
    "limits": [
      "JPEG, WebP, EXIF orientation, corrupted images, animation, color-profile preservation, metadata preservation, partial-write failures, and arbitrary fractional-dimension rounding cases were not tested."
    ]
  },
  "references": [
    {
      "title": "Official Pillow documentation · Image module",
      "url": "https://pillow.readthedocs.io/en/stable/reference/Image.html"
    },
    {
      "title": "Official Pillow documentation · ImageOps module",
      "url": "https://pillow.readthedocs.io/en/stable/reference/ImageOps.html"
    },
    {
      "title": "Official Pillow documentation · Image file formats",
      "url": "https://pillow.readthedocs.io/en/stable/handbook/image-file-formats.html"
    },
    {
      "title": "Official Python 3.12 documentation · pathlib",
      "url": "https://docs.python.org/3.12/library/pathlib.html"
    }
  ],
  "slug": "batch-image-resize",
  "category": "automation",
  "date": "2026-09-20"
}
