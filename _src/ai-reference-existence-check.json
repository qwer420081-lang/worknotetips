{
  "title": "Check Whether AI-Cited References Actually Exist",
  "summary": "AI-generated references can look plausible even when a link is missing or the cited page does not support the claim. This guide uses a small synthetic example to check both URL existence and whether the referenced page contains evidence related to the AI's claim.",
  "seo_title": "Check AI References and Links with Python",
  "seo_desc": "Check whether AI-cited links exist and whether the referenced pages support the claims, using a small Python script and review checklist.",
  "tags": [
    "AI verification",
    "references",
    "Python"
  ],
  "audience": "For people who use an AI chat assistant for research, reports, or documentation and want to verify cited references before relying on them.",
  "prerequisites": [
    "Python 3.12",
    "Internet access when running the URL-checking script"
  ],
  "sections": [
    {
      "id": "why-check-ai-references",
      "heading": "Why check AI-generated references?",
      "blocks": [
        {
          "type": "p",
          "text": "A reference that looks professional is not automatically reliable. An AI response may contain a real URL with an inaccurate description, a URL that no longer exists, or a completely fabricated reference. Verification therefore has two separate questions: does the reference exist, and does it actually say something that supports the AI's claim?"
        },
        {
          "type": "ul",
          "items": [
            "Existence check: can the URL be reached and does it return a normal web response?",
            "Content check: does the retrieved page contain information relevant to the claimed point?",
            "Source check: is the page really from the organization, publisher, or documentation site the AI named?",
            "Human review: does the page support the full claim rather than merely sharing a few keywords?"
          ]
        },
        {
          "type": "callout",
          "title": "Do not treat a working link as proof",
          "text": "HTTP success only shows that a resource was reached. It does not prove that the AI summarized the resource correctly."
        }
      ]
    },
    {
      "id": "synthetic-example",
      "heading": "Use a tiny synthetic example",
      "blocks": [
        {
          "type": "p",
          "text": "The following example is synthetic. Imagine asking an AI chat assistant which Python standard-library pages explain opening URLs and working with JSON. You want to verify the answer before putting the references into a work note."
        },
        {
          "type": "p",
          "text": "Prompt to the AI: Give me official Python references for opening URLs and reading or writing JSON. For each reference, state one specific claim that the page supports and provide the exact URL."
        },
        {
          "type": "table",
          "headers": [
            "Reference",
            "AI claim",
            "URL"
          ],
          "rows": [
            [
              "urllib.request",
              "Python provides urllib.request for opening URLs.",
              "https://docs.python.org/3/library/urllib.request.html"
            ],
            [
              "json",
              "Python's json module provides JSON encoding and decoding.",
              "https://docs.python.org/3/library/json.html"
            ],
            [
              "Synthetic fake reference",
              "This page documents an AI-specific reference verifier.",
              "https://example.invalid/fake-reference"
            ]
          ]
        },
        {
          "type": "p",
          "text": "The third row is deliberately fake for this synthetic exercise. The goal is to make the verification process detect it rather than trusting the formatting of the citation."
        }
      ]
    },
    {
      "id": "prepare-check-list",
      "heading": "Turn the AI answer into explicit checks",
      "blocks": [
        {
          "type": "p",
          "text": "Do not ask Python to decide whether an entire natural-language claim is true. Instead, record the URL, the claim, and a few terms that should reasonably appear if the page is relevant. The terms are only a screening aid."
        },
        {
          "type": "table",
          "headers": [
            "URL",
            "Terms to look for"
          ],
          "rows": [
            [
              "https://docs.python.org/3/library/urllib.request.html",
              "urllib.request; URL"
            ],
            [
              "https://docs.python.org/3/library/json.html",
              "json; encoding; decoding"
            ],
            [
              "https://example.invalid/fake-reference",
              "reference; verifier"
            ]
          ]
        },
        {
          "type": "ol",
          "items": [
            "Copy each URL exactly as the AI returned it.",
            "Write the claim beside the URL instead of checking the URL alone.",
            "Choose only a few distinctive screening terms.",
            "After the automated check, open every reference you intend to cite and read the relevant passage yourself."
          ]
        }
      ]
    },
    {
      "id": "python-check",
      "heading": "Check the URLs and page text with Python",
      "blocks": [
        {
          "type": "p",
          "text": "This standard-library script requests each page, records the final URL and HTTP status, extracts basic visible text, and checks the selected terms. It writes a report to outputs/reference_check.json. To avoid overwriting a previous review, it stops if the outputs directory already exists."
        },
        {
          "type": "code",
          "text": "from pathlib import Path\nfrom urllib.request import Request, urlopen\nfrom urllib.error import HTTPError, URLError\nimport html\nimport json\nimport re\n\nchecks = [\n    {\n        \"reference\": \"urllib.request\",\n        \"url\": \"https://docs.python.org/3/library/urllib.request.html\",\n        \"claim\": \"Python provides urllib.request for opening URLs.\",\n        \"terms\": [\"urllib.request\", \"URL\"],\n    },\n    {\n        \"reference\": \"json\",\n        \"url\": \"https://docs.python.org/3/library/json.html\",\n        \"claim\": \"Python's json module provides JSON encoding and decoding.\",\n        \"terms\": [\"json\", \"encoding\", \"decoding\"],\n    },\n    {\n        \"reference\": \"Synthetic fake reference\",\n        \"url\": \"https://example.invalid/fake-reference\",\n        \"claim\": \"This page documents an AI-specific reference verifier.\",\n        \"terms\": [\"reference\", \"verifier\"],\n    },\n]\n\noutput_dir = Path(\"outputs\")\nif output_dir.exists():\n    raise SystemExit(\"outputs already exists; remove or rename it before running again\")\noutput_dir.mkdir()\n\nresults = []\n\nfor item in checks:\n    result = {\n        \"reference\": item[\"reference\"],\n        \"url\": item[\"url\"],\n        \"claim\": item[\"claim\"],\n        \"reachable\": False,\n        \"status\": None,\n        \"final_url\": None,\n        \"term_matches\": {},\n        \"error\": None,\n    }\n\n    try:\n        request = Request(\n            item[\"url\"],\n            headers={\"User-Agent\": \"Worknote reference checker/1.0\"},\n        )\n        with urlopen(request, timeout=10) as response:\n            result[\"status\"] = response.status\n            result[\"final_url\"] = response.geturl()\n            raw = response.read(500_000).decode(\"utf-8\", errors=\"replace\")\n\n        text = re.sub(r\"<script.*?</script>\", \" \", raw, flags=re.I | re.S)\n        text = re.sub(r\"<style.*?</style>\", \" \", text, flags=re.I | re.S)\n        text = re.sub(r\"<[^>]+>\", \" \", text)\n        text = html.unescape(text)\n        text = re.sub(r\"\\s+\", \" \", text)\n\n        result[\"reachable\"] = True\n        lowered = text.casefold()\n        result[\"term_matches\"] = {\n            term: term.casefold() in lowered for term in item[\"terms\"]\n        }\n\n    except HTTPError as exc:\n        result[\"status\"] = exc.code\n        result[\"error\"] = f\"HTTPError: {exc}\"\n    except URLError as exc:\n        result[\"error\"] = f\"URLError: {exc.reason}\"\n    except Exception as exc:\n        result[\"error\"] = f\"{type(exc).__name__}: {exc}\"\n\n    results.append(result)\n\nreport_path = output_dir / \"reference_check.json\"\nreport_path.write_text(\n    json.dumps(results, indent=2, ensure_ascii=False),\n    encoding=\"utf-8\",\n)\n\nfor result in results:\n    print(result[\"reference\"])\n    print(\"  reachable:\", result[\"reachable\"])\n    print(\"  status:\", result[\"status\"])\n    print(\"  final URL:\", result[\"final_url\"])\n    print(\"  term matches:\", result[\"term_matches\"])\n    print(\"  error:\", result[\"error\"])\n\nprint(f\"Saved: {report_path}\")"
        }
      ]
    },
    {
      "id": "review-results",
      "heading": "Interpret the result conservatively",
      "blocks": [
        {
          "type": "p",
          "text": "A reachable page with matching terms should be labeled for manual review, not automatically marked as a verified claim. Keyword matching cannot determine context, scope, negation, or whether the AI exaggerated what the source says."
        },
        {
          "type": "table",
          "headers": [
            "Result",
            "Interpretation",
            "Next action"
          ],
          "rows": [
            [
              "Reachable and terms found",
              "The page may be relevant.",
              "Read the relevant section and compare it with the exact AI claim."
            ],
            [
              "Reachable but terms missing",
              "The URL exists, but the quick content screen did not find the expected wording.",
              "Inspect the page manually and check whether the AI cited the wrong page."
            ],
            [
              "HTTP error",
              "The server responded with an error status.",
              "Check the URL, redirects, access requirements, or whether the reference moved."
            ],
            [
              "Network or DNS error",
              "The script could not reach the address.",
              "Check spelling and verify the reference independently."
            ]
          ]
        }
      ]
    },
    {
      "id": "manual-checklist",
      "heading": "Perform the final manual check",
      "blocks": [
        {
          "type": "ol",
          "items": [
            "Confirm the page title and organization match what the AI named.",
            "Search the page for the exact concept used in the claim.",
            "Read enough surrounding text to understand qualifications and exceptions.",
            "Check whether the AI changed a narrow statement into a broader conclusion.",
            "Record the relevant section heading or passage for later review.",
            "Reject or correct any citation whose source cannot be located or does not support the stated claim."
          ]
        },
        {
          "type": "callout",
          "title": "Check claims, not just citations",
          "text": "A real document can still be cited incorrectly. The strongest workflow keeps the AI's exact claim beside the source and reviews the two together."
        }
      ]
    },
    {
      "id": "common-mistakes-and-limits",
      "heading": "Common mistakes and limits",
      "blocks": [
        {
          "type": "ul",
          "items": [
            "Assuming a familiar domain means every URL on that domain is genuine.",
            "Accepting a page because one keyword appears somewhere in it.",
            "Ignoring redirects and checking only the originally supplied URL.",
            "Treating a page title, search snippet, abstract, or table of contents as evidence for a detailed claim.",
            "Using automated text matching as a substitute for reading the cited passage.",
            "Forgetting that some valid sites block automated requests or require JavaScript, authentication, or other access methods."
          ]
        },
        {
          "type": "p",
          "text": "For academic work, also verify bibliographic fields such as authors, title, year, journal, DOI, and the actual paper text. A URL checker cannot establish scholarly validity, detect retracted work, or decide whether evidence is methodologically strong."
        }
      ]
    }
  ],
  "verification": {
    "env": "ChatGPT Python tool · Python 3.x · standard library only",
    "items": [
      "The script completed successfully and created outputs/reference_check.json.",
      "The output contained exactly three result objects corresponding to urllib.request, json, and the synthetic fake reference.",
      "Because the Python execution environment could not resolve external DNS, all three requests were caught as URLError values rather than crashing the script.",
      "For unreachable URLs, reachable remained false, status and final_url remained null, term_matches remained empty, and the error field recorded the network failure as intended.",
      "The script uses only Python standard-library modules: pathlib, urllib.request, urllib.error, html, json, and re.",
      "The script did not overwrite an existing review: a second execution stopped with 'outputs already exists; remove or rename it before running again'.",
      "The generated report filename was exactly outputs/reference_check.json, matching the article."
    ],
    "limits": [
      "The ChatGPT Python execution environment had no working outbound DNS/network access during verification, so current HTTP status codes, redirects, downloaded page text, and term matches for the two Python documentation URLs could not be verified by executing this script.",
      "Because no pages were retrieved, the live reachable-and-terms-found branch could not be exercised.",
      "Keyword presence would only screen for possible relevance and would not establish that a cited page semantically supports the full claim.",
      "An unrelated spreadsheet-runtime warmup warning was emitted during Python startup; it did not affect execution of the article script."
    ]
  },
  "references": [
    {
      "title": "Python documentation: urllib.request — Extensible library for opening URLs",
      "url": "https://docs.python.org/3/library/urllib.request.html"
    },
    {
      "title": "Python documentation: json — JSON encoder and decoder",
      "url": "https://docs.python.org/3/library/json.html"
    }
  ],
  "slug": "ai-reference-existence-check",
  "category": "ai",
  "date": "2026-09-21"
}
